export default class CalculatorModel {
  #firstOperand;
  #SecondOperand;
  #operator;

  constructor() {}

  calculate(formula) {
    try {
      //operator 받아들이는 함수
      //operand 받아들이는 함수
    } catch (error) {
      //에러 처리
    }
    //결과값 리턴
  }
}
